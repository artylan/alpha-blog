<h1>Listing Articles page</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th colspan="3">Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @articles.each do |article| %>
      <tr>
        <td><%= article.title %></td>
        <td><%= article.description %></td>
        <td><%= link_to "Zeigen", article_path(article) %></td>
        <td><%= link_to "Editieren", edit_article_path(article) %></td>
        <td><%= link_to "Löschen", article_path(article), method: :delete, data: { confirm: "Sind Sie sicher?" } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<p>
  <%= link_to "Neuen Artikel erstellen", new_article_path %>
</p>